[project]
name = "whatsapp-mcp-server"
version = "0.1.0"
description = "Local-first MCP server for WhatsApp, powered by WhatsMeow"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.28.1",
    "mcp[cli]>=1.6.0",
    "requests>=2.32.3",
]
